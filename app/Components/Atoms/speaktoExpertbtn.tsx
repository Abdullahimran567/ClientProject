import React from 'react';
import { Button } from '../ui/button';
import GetAQuoteForm from '../Forms/GetAQuoteForm';
function SpeaktoExpertbtn() {
  return (
    <Button variant='Custom1' className='cursor-pointer '>
      Speak To Expert
    </Button>
  );
}

export default SpeaktoExpertbtn;
